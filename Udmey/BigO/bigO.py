
def BigO(number_operation):
    for i in range(number_operation):
        for j in range(number_operation):
            print(i , j)

    for k in range(number_operation):
        print(k)

def findElement(Element):
    division = 2
    array = [1,2,3,4,5,6,7,8,]
    for 
    for val in array:


 

if __name__ == "__main__":
    """
    now in this case we can Drop Mon-Domainat O(n)
    which in this function Operations takes as follow:
    
    - instead Loop of (i ,j) --> O(n^2)
    - k loop is --> O(n)

    the Big Complexcity of this function is : BigO(N)== (N^2 + N)

    but the domnainat operation is N^2 which N isn't impact on operation which could make it as folllow : --> bigO is O(N^2)

    """
    case = BigO(10)
